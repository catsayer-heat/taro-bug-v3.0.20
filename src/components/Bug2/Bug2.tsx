import React, { useState, useEffect, useCallback } from 'react'
import { Button } from '@tarojs/components'

const Page: React.FC = () => {
  return (
    <React.Fragment>
      <Button
        onClick={() => {
        }}
      >
        bug2
      </Button>
    </React.Fragment>
  )
}

export default Page